<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/HiddenTitleTheme">
        <activity android:name=".facts" />
        <activity android:name=".Profile" />
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <!-- PhotoRecognition activity -->
        <activity
            android:name=".PhotoRecognition"
            android:exported="true">
            <!-- If you still need an intent filter here, add it; otherwise, keep it as is -->
        </activity>

        <activity
            android:name=".DatabaseManagementActivity"
            android:exported="true">
            <!-- If you still need an intent filter here, add it; otherwise, keep it as is -->
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- If you still need an intent filter here, add it; otherwise, keep it as is -->
        </activity>
        <activity
            android:name=".NewDish"
            android:label="New Dish"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.speakcalproject.MainActivity" />
        </activity>
        <activity
            android:name=".MealsActivity"
            android:label="Meals"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.speakcalproject.MainActivity" />
        </activity>
        <activity
            android:name=".GroceriesActivity"
            android:label="Groceries"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.speakcalproject.MainActivity" />
        </activity>
        <activity
            android:name=".RecipeActivity"
            android:label="Recipes"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.speakcalproject.MainActivity" />
        </activity>
        <activity
            android:name=".RecipeViewActivity"
            android:label="Recipes"
            android:parentActivityName=".RecipeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.speakcalproject.RecipeActivity" />
        </activity>
        <activity android:name="com.soundcloud.android.crop.CropImageActivity" />
        <activity android:name=".NutriDataActivity" />
        <activity
            android:name=".AndroidDatabaseManager"
            android:label="@string/app_name" />
    </application>

</manifest>
